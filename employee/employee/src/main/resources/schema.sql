CREATE TABLE `employee`.`tbl_employee` (
                                           `id` INT NOT NULL,
                                           `name` VARCHAR(45) NOT NULL,
                                           `email` VARCHAR(45) NOT NULL,
                                           `department` VARCHAR(45) NOT NULL,
                                           PRIMARY KEY (`id`));